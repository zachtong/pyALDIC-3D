"""Main-window panels (Qt view layer): left sidebar | canvas area | right sidebar.

Same three-column UX grammar as the 2D app; the content is 3D-DIC (dual-camera
import, calibration, correspondence strategy, 3D fields).
"""
